// function na(name) {
//   console.log(`helo kdl ${name}`); //      old way defining functions
// }

// na(k);
// na(l);                            //      old way answer
// na("hjfgsadb");

const hi = (name) => {
  // new way of defining    function
  console.log(`hello klh ${name}`);
};

module.exports = hi;
